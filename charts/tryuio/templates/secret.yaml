{{- if not .Values.apiKey }}
{{- fail "apiKey is required. Pass it with --set apiKey=<your-api-key>" }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  api-key: {{ .Values.apiKey | quote }}
